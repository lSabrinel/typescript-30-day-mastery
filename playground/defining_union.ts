function printNumsAndStrings(statement: string | number) { // I added the union
  console.log(`ℹ️ LOG:: ${statement}`);
}

// Write the function call here

printNumsAndStrings('hello!');
printNumsAndStrings(2);